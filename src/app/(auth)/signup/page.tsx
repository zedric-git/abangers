export default function SignupPage() {
  // TODO: sign-up form with role selection (renter/landlord).
  // On success: create the auth user, then insert a matching row into
  // `profiles` with the chosen role (see supabase/migrations/0001_init.sql).
  return <div>Signup page — TODO</div>;
}
