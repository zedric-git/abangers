export default function LoginPage() {
  // TODO: Supabase email/password + Google OAuth sign-in form.
  // On success, redirect based on profiles.role (renter -> /dashboard/renter,
  // landlord -> /dashboard/landlord).
  return <div>Login page — TODO</div>;
}
