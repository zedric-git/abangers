export default async function ListingsPage() {
  // TODO: fetch listings via the server Supabase client (src/lib/supabase/server.ts),
  // render as cards, add search/filter controls (Phase 1 scope: location, price, amenities).
  return <div>Listings page — TODO</div>;
}
