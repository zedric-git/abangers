export default function NewListingPage() {
  // TODO: React Hook Form + zodResolver(listingSchema) from
  // src/lib/validations/listing.ts. Submit via a Server Action that
  // re-validates with the same schema before inserting.
  return <div>Add listing form — TODO</div>;
}
