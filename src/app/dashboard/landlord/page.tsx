export default function LandlordDashboardPage() {
  // TODO: list this landlord's own listings (RLS scopes the query
  // automatically once auth is wired up) + inquiry inbox.
  return <div>Landlord dashboard — TODO</div>;
}
